spring:
  application:
    name: order-events-producer
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}  # override via env var SPRING_PROFILES_ACTIVE=stage|prod
#    active: dev          # override via -Dspring.profiles.active=stage|prod at runtime

  kafka:
    # bootstrap-servers and topic are set per-profile (see application-{dev|stage|prod}.yml)
    producer:
      acks: all
      retries: 10
      key-serializer: org.apache.kafka.common.serialization.LongSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      properties:
        retry.backoff.ms: 1000
        enable.idempotence: true
        max.in.flight.requests.per.connection: 5

app:
  api:
    base-path: /api/orderevent

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true               # enables /actuator/health/liveness and /actuator/health/readiness
        add-additional-paths: true  # also exposes /livez and /readyz at the root
      show-details: always
      group:
        liveness:
          include: livenessState, kafkaReadiness   # liveness fails when Kafka is unreachable (e.g. due to network issues or broker downtime)
        readiness:
          include: readinessState,kafkaReadiness
  health:
    livenessstate:
      enabled: true
    readinessstate:
      enabled: true

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html